extends layout

block content
  div.banner
    i(class="#{ icon }") &nbsp; #{ status }

  h1 Riddle 3

  i.icon-lock
  label(for="password") Random password:
  input(name="password", type="password", value="yt!ruc35")

  p Hmm, that password might be worth remembering.

  // try /ziggy

  script console.log('Seriously, make a note of that password.');